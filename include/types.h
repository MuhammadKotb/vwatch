#pragma once


namespace vw {
    struct Color {
        float r, g, b;
    };

    struct ColorNorm {
        float r, g, b;
    };
    struct Triangle {
        float x0, y0, x1, y1, x2, y2;
    };
}